# Google TV 家庭學習遊戲 — Codex 專案交接文件

> 最後整理日期：2026-08-18
>
> 目的：讓 Codex 可以直接接手目前的產品規劃與既有程式，不需要重新詢問前面的討論背景。

---

## 1. 專案一句話說明

這是一個以 **Google TV / Android TV 遙控器** 為主要操作方式的家庭學習遊戲平台。

第一個遊戲是給小朋友練習 **九九乘法**。未來會擴充成包含加減法、英文單字、國字等內容的家庭學習平台。

這不是只為桌面瀏覽器設計的網站；**TV-first UX** 是核心需求。

---

## 2. 使用情境

主要使用者是一個家庭，家中約 4 位成員可能共用同一台 Google TV。

目前最主要的學習需求：

- 小朋友已經可以順著背九九乘法。
- 但如果題目隨機出現，反應會比較慢。
- 希望透過遊戲化、隨機出題的方式強化真正的記憶，而不是只會照順序背。

典型流程：

1. 在 Google TV 首頁打開瀏覽器或之後的 App / PWA。
2. 選擇既有玩家。
3. 選擇遊戲模式和時間。
4. 用遙控器方向鍵選答案，按 OK 確認。
5. 每題回答後立刻看正確答案。
6. 玩家自己按 OK 才進下一題。
7. 計時結束後看到本局成績與歷史最高分。

---

## 3. UX 核心原則

### 必須做到

- 全螢幕／接近全螢幕的 TV 體驗。
- 橫向 16:9 電視畫面優先。
- 字體非常大。
- 按鈕大。
- 介面簡潔。
- 高對比。
- 目前被遙控器選中的項目必須有非常明顯的 focus 樣式。
- 所有核心功能都必須能只靠：
  - 上
  - 下
  - 左
  - 右
  - OK / Enter
  - Back
  完成。
- 不可把滑鼠 hover 當成必要互動。
- 不要依賴 touch 操作。

### 語言

**所有 UI 使用繁體中文。**

不要改成簡體中文。

### 視覺方向

- 簡潔，不要塞太多資訊。
- 可以有一些小朋友喜歡的回饋效果，但不要花俏到干擾答題。
- 答對可以是清楚、正向的回饋。
- 答錯要溫和，不要製造挫折感。

---

## 4. 首頁／平台架構

首頁不要寫死成「九九乘法單頁」。

它應該像一個家庭學習平台的首頁，保留多個遊戲入口。

第一版可顯示：

- 九九乘法 — 可玩
- 加減法 — 敬請期待
- 英文單字 — 敬請期待
- 國字練習 — 敬請期待

未來應該能用模組化方式加入更多遊戲，而不需要重寫共用的玩家系統、設定系統或導覽。

---

## 5. 玩家系統

### 已決定需求

家中會有多位玩家。

第一次可新增玩家名稱。之後名字應該成為首頁上的玩家選項，不需要每次重新輸入。

每位玩家需要獨立記錄：

- 玩家名稱
- 歷史最高分
- 最近一次成績
- 遊玩次數
- 各題答對次數
- 各題答錯次數

玩家管理需要：

- 新增玩家
- 選擇玩家
- 清除某玩家的成績紀錄
- 刪除玩家

### 儲存方式

第一版 **不要建立後端或資料庫**。

使用瀏覽器 `localStorage` 即可。

原因：

- 主要就是同一台 Google TV 使用。
- 不需要登入帳號。
- 不需要跨裝置同步。
- 希望部署和維護越簡單越好。

未來若需要跨電視／手機同步，再考慮加入後端。

### 注意

清除瀏覽器網站資料會讓 Local Storage 消失。未來可以考慮增加「備份／匯出玩家資料」功能，但不是第一版必要項目。

---

## 6. 九九乘法遊戲規格

### 題目範圍

目前設定為：

- `2 × 2` 到 `9 × 9`

任何一邊為 1 的乘法都不出題。

### 題目順序

- 隨機出題。
- 不按照九九乘法表順序。

### 錯題加權

希望系統不是永遠完全平均隨機。

玩家常答錯的題目應該提高再次出現的機率。

已經很熟的題目可以稍微降低出現機率，但不要完全消失。

這是長期很重要的學習機制。

---

## 7. 遊戲模式

### 模式 A：四選一

範例：

`6 × 7 = ?`

下方顯示 4 個大型答案。

需求：

- 只有一個正確答案。
- 干擾答案不能太荒謬。
- 優先產生接近正確答案的數字，讓孩子真的需要記憶答案，而不是用排除法秒選。
- 方向鍵移動答案。
- OK 確認。

### 模式 B：數字輸入（進階）

同樣顯示：

`6 × 7 = ?`

但不給選項。

畫面顯示大型的 0–9 數字鍵與確認鍵，玩家使用遙控器輸入完整答案，例如 `42`。

這個模式是未來從「辨認答案」進階到「主動回憶答案」的重要功能。

---

## 8. 計時

每局固定為 **60 秒**，不再提供其他時間選項。遊戲畫面需清楚顯示剩餘秒數與倒數進度條。

答題回饋期間暫停倒數，玩家按 OK 進入下一題後才繼續。若答錯扣秒後時間歸零，仍需先顯示正確答案，玩家按 OK 後才進入結果頁。

---

## 9. 每題答題流程

這是很重要的學習需求：

1. 顯示題目。
2. 玩家選答案並按 OK。
3. **立即顯示正確答案。**
4. 如果答錯，也一定要清楚顯示正確答案。
5. 不要自動立刻跳下一題。
6. 讓小朋友自己按 OK / Enter 再進下一題。

原因：

希望小朋友有一點時間重新看一次「題目 → 正確答案」，幫助記憶。

---

## 10. 計分

目前第一版採簡單規則：

- 答對：+10 分
- 答錯：0 分
- 分數不倒扣，但答錯會扣 5 秒

一局結束至少顯示：

- 本輪分數
- 答對題數
- 答錯題數
- 正確率
- 歷史最高分

之後可考慮：

- 連續答對 combo
- 個人最佳紀錄
- 家庭排行榜

但不要在第一版過度複雜化。

---

## 11. 音效

討論過加入：

- 答對：輕快提示音
- 答錯：溫和提示音

原則：

- 不要太吵。
- 最好可以關閉。
- 不應成為第一版核心流程的 blocker。

目前程式 **尚未真正完成音效**。

---

## 12. Google TV / Android TV 操作

核心控制方式：

- Arrow Up → 上
- Arrow Down → 下
- Arrow Left → 左
- Arrow Right → 右
- Enter / OK → 確認
- Back / Escape / BrowserBack → 返回

### Focus Navigation

不能只相信瀏覽器預設 Tab 順序。

應有 TV 專用 spatial navigation 邏輯，根據元素的畫面位置尋找上下左右最合理的下一個 focus target。

目前第一版已有一個基於 element bounding box 的簡單實作。

實機 Google TV 測試仍是必要的。

### 瀏覽器

Google TV 不應假設一定有官方 Chrome 可直接啟動。

目前規劃開發／測試階段：

- 電腦：Chrome 等一般桌面瀏覽器開發。
- Google TV：先使用 TV Bro 類型的 TV 瀏覽器測試。

長期目標是減少「先開瀏覽器再打網址」的操作成本。

---

## 13. 部署方向

### 第一階段

使用 **GitHub Pages**。

目標：

- 純靜態網站。
- push 後即可部署。
- Google TV 以固定網址開啟。
- 不需要伺服器。
- 不需要資料庫。

### 第二階段：PWA

計畫加入：

- `manifest.webmanifest`
- icon
- service worker
- standalone display mode
- 基本離線快取

目的：

讓支援的裝置／瀏覽器可以有更接近 App 的啟動體驗。

**注意：不要假設所有 Google TV 瀏覽器都支援「安裝 PWA 到首頁」。實機能力要驗證。**

### 第三階段（若 PWA 體驗不夠好）

考慮把同一套 Web app 包成 Android TV App，讓它可以：

- 正式出現在 Google TV App 清單／首頁。
- 點 icon 直接進入。
- 遙控器 Back / D-pad 行為更可控。

這不是目前第一優先。

---

## 14. 技術方向

目前刻意保持低依賴：

- HTML
- CSS
- Vanilla JavaScript
- Local Storage

暫時不要為了架構漂亮就立刻改成 React / Vue / Angular。

除非未來功能規模真的需要，再評估框架。

理由：

- 功能目前不複雜。
- 靜態部署容易。
- Google TV 瀏覽器相容性比較單純。
- 首屏載入快。
- debug 容易。

---

## 15. 現有專案狀態

目前專案已有以下檔案：

```text
/
├── index.html
├── styles.css
├── app.js
└── README.md
```

現有版本已實作大部分 MVP：

- 家庭學習平台首頁
- 九九乘法入口
- 多玩家
- Local Storage
- 玩家選擇
- 玩家最高分
- 最近一次分數
- 遊玩次數
- 清除紀錄
- 刪除玩家
- 固定 60 秒與可視化倒數
- 四選一模式
- 數字輸入模式
- 2–9 乘法隨機出題（排除任一乘數為 1）
- 錯題加權
- 接近正確答案的干擾選項
- 答題後顯示答案
- 玩家按 OK 後下一題
- TV 遙控器方向鍵 focus navigation
- 結果頁
- 繁體中文 UI

---

## 16. 現有資料模型

目前 Local Storage key：

```js
family-learning-tv-v1
```

概念上的 state：

```js
{
  players: [
    {
      id,
      name,
      highScore,
      lastScore,
      plays,
      questionStats: {
        "6x7": {
          correct: 3,
          wrong: 2
        }
      }
    }
  ],
  currentPlayerId,
  settings: {
    duration: 60,
    mode: "choice"
  }
}
```

如果未來修改資料結構，請考慮 migration，不要直接讓既有玩家資料全部失效。

---

## 17. 目前實作的錯題加權概念

每一題都有 weight。

目前大致邏輯：

- 基礎 weight = 1
- 每答錯一次，大幅提高 weight
- 答對會稍微降低 weight
- weight 仍有最低值，因此熟悉題目仍可能出現

這個方向符合需求。

未來可以改善，但不要拿掉「錯題優先」的核心原則。

---

## 18. 現有程式需要優先驗證的地方

請 Codex 接手後先 **讀程式、跑起來、實際測試，再重構**。

優先檢查：

1. Google TV / TV Bro 的 D-pad key event 是否和桌面鍵盤一致。
2. Google TV 遙控器 OK 是否映射為 `Enter`。
3. Back 是否為 `BrowserBack`、`Escape`、Android key code 或瀏覽器 history 行為。
4. `<dialog>` 在目標 TV 瀏覽器的相容性。
5. `crypto.randomUUID()` fallback 是否足夠。
6. `structuredClone()` 在目標 TV 瀏覽器是否可用；若相容性不佳，換成安全的簡單 fallback。
7. Focus navigation 在 dialog、首頁 grid、2×2 答案與 keypad 上是否自然。
8. 答題後按 Enter 進下一題時，是否可能和目前 focused button 的 click 造成雙觸發。
9. 60 秒到時剛好正在 feedback 狀態，是否能安全 finish。
10. 離開遊戲時 timer 是否一定會清乾淨。
11. 重新整理頁面後 Local Storage 是否正常恢復。
12. 玩家清除／刪除後 current player 狀態是否正確。
13. 數字輸入模式在 TV 遙控器上的實際效率與 focus path。

---

## 19. 下一階段建議工作順序

### P0 — 先讓 TV 實機穩定

- 建立簡單本機 server / GitHub Pages preview。
- 在 Google TV + TV Bro 實機測試。
- 修正 D-pad / OK / Back。
- 修正 focus UX。
- 檢查 1080p 與可能的 4K 顯示比例。

### P1 — 整理架構但不要過度工程化

可以把共用邏輯逐步拆分，例如：

```text
/
├── index.html
├── styles/
│   └── main.css
├── js/
│   ├── app.js
│   ├── storage.js
│   ├── navigation.js
│   ├── players.js
│   └── games/
│       └── multiplication.js
├── manifest.webmanifest
└── README.md
```

但只有在拆分後真的更好維護時才做。

### P2 — PWA

- manifest
- app icon
- service worker
- standalone
- offline static assets

### P3 — 學習體驗改善

- 錯題複習模式
- 更好的 adaptive weighting
- 玩家弱項統計
- 家庭排行榜
- 音效開關

### P4 — 新遊戲

- 加減法
- 除法
- 英文單字
- 國字

---

## 20. 不要做的事情

除非有明確理由，暫時不要：

- 引入登入系統。
- 建資料庫。
- 建後端 API。
- 改成大型 JS framework。
- 加廣告。
- 加追蹤器／analytics。
- 把 UI 改成手機優先。
- 依賴滑鼠 hover。
- 為了炫技加入大量動畫。
- 自動把答錯題目跳過而不顯示正確答案。
- 每次進遊戲都要求重新輸入玩家名稱。
- 把繁體中文改成簡體中文。

---

## 21. 尚未決定／需要未來討論

以下項目不要自行做重大產品決策：

- 一局中是否允許重複同一題很快再次出現。
- `a × b` 和 `b × a` 要視為同一個學習項目還是兩個不同題目。
- 是否要加入「只練某一個乘數」模式，例如只練 7 的乘法。
- 是否要有無限練習、不計時模式。
- 是否需要真正的家庭排行榜。
- 是否要讓家長設定 PIN。
- 是否需要資料備份／匯出／匯入。
- PWA 是否能在實際 Google TV launcher 上提供理想的啟動體驗。
- 若 PWA 不夠好，要用 WebView / Trusted Web Activity / 原生 Android TV shell 哪一種包裝方式。

---

## 22. Codex 接手時的第一個任務

請先不要大重構。

第一輪建議任務：

1. 閱讀 `README.md`、本文件、`index.html`、`styles.css`、`app.js`。
2. 啟動現有專案。
3. 做一次完整 code review。
4. 找出會影響 Google TV / TV Bro 的相容性問題。
5. 找出鍵盤／遙控器 navigation 的 edge cases。
6. 修正明確 bug。
7. 保持現有產品行為，除非 bug 修正一定需要改變。
8. 完成後列出：
   - 修改內容
   - 為什麼修改
   - 如何測試
   - 還有哪些項目需要 Google TV 實機驗證

---

## 23. 可以直接貼給 Codex 的起始 Prompt

```text
請接手這個 Google TV 家庭學習遊戲專案。

先完整閱讀 CODEX_HANDOFF.md、README.md，以及目前所有 HTML/CSS/JS，不要立刻重寫。

第一階段目標不是增加新功能，而是：
1. 確認現有 MVP 可以正常執行。
2. Review 所有程式碼。
3. 優先找出 Google TV / Android TV / TV Bro 上可能發生的相容性問題。
4. 特別檢查 D-pad spatial navigation、OK/Enter、Back、dialog、Local Storage、計時器與答題後再按 OK 進下一題的流程。
5. 修正明確 bug，但不要自行改變 CODEX_HANDOFF.md 中尚未決定的產品行為。
6. 保持繁體中文 UI。
7. 暫時不要引入 React/Vue、後端或資料庫。

完成第一輪後，請告訴我：
- 你發現了哪些問題
- 你修改了哪些檔案
- 每個修改的原因
- 我應該如何在電腦與 Google TV 上測試
- 下一步你建議做什麼
```

---

## 24. 建議新增 AGENTS.md

當這個 repo 正式交給 Codex 長期維護後，建議在 repository root 新增 `AGENTS.md`，放「長期不太會變的開發規則」。

例如：

- UI 一律繁體中文。
- TV-first。
- D-pad 全功能可操作。
- 不依賴 hover。
- Vanilla JS 優先。
- 不自行新增後端。
- 修改前先理解既有 localStorage schema。
- 新遊戲需共用玩家系統與導航系統。
- 所有 TV navigation 修改都必須附手動測試步驟。

`CODEX_HANDOFF.md` 比較適合保存「產品背景與目前計畫」；`AGENTS.md` 則比較適合保存「Codex 每次工作都必須遵守的 repo 規則」。

---

## 25. 最重要的產品精神

這個產品的重點不是做一個很炫的數學網站。

重點是：

> 小朋友坐在電視前，只拿一支 Google TV 遙控器，就可以很直覺地開始練習，而且答錯後能清楚看到正確答案，逐漸把原本只能順著背的九九乘法，變成真正可以隨機回憶的知識。

所有技術選擇都應服務這個目的。
